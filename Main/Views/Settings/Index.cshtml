@using SDM.Main.Views.Settings
@inherits WebViewPage<dynamic>, ILocalizable<IndexTexts>
@{
    ViewBag.Title = this.Localize(t => t.Shared.Settings);
}

<fieldset>
    <legend>Account information</legend>
    @Html.Partial("Index.ChangePassword")
</fieldset>